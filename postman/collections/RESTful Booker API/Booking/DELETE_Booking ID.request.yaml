$kind: http-request
name: DELETE_Booking ID
url: "{{base_url}}/booking/{{booking_id}}"
method: DELETE
headers:
  Cookie: token={{token}}
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 201\", function () {\r

      \    pm.response.to.have.status(201);\r

      });"
    language: text/javascript
order: 4000
